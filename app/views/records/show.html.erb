<table>
  <th>
    <div class="card col-md-4">
      <%= render 'users/icon' %>
      <div><%= @record.title %></div>
    </div>
  </th>
  <th>
    <div class="card col-md-8">
      <%= image_tag @record.photo.thumb400.url %>
      <div class="card-body">
        <p class="card-text"><%= @record.content %></p>
      </div>
    </div>
  </th>
</table>
<%= render 'likes/like_form' %>
<%= link_to '投稿を削除する', record_path, method: :delete %>
