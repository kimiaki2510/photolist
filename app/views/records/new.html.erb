<h1>良い写真取れましたか?</h1>
<%= form_with(model: @record, local: true)do |f|%>
  <%= f.label :title, 'タイトル'%>
  <%= f.text_field :title %>
  <%= f.label :content, '感想' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿する' %>
<% end %>
<%= link_to 'タイムラインに戻る', root_url %>
