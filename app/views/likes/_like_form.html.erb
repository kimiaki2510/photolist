<!-- いいね判断 -->
<div class="like_form">
  <% if current_user.already_liked?(record) %>
    <%= render 'likes/unlike', record: record %>
  <% else %>
    <%= render 'likes/like', record: record %>
  <% end %>
</div>
