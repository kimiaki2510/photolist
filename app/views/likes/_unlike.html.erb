<%= form_for(current_user.likes.find_by(reord_id: @record.id), html: {method: :delete},remote: true )do |f| %>
  <%= hidden_field_tag :record_id, record.id %>
  <%= f.submit 'いいね解除', class: 'btn btn-danger' %>
<% end %>
